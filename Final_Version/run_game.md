# Quick Start

```bash
python -m venv .venv
```

```bash
pip install -r requirements.txt
```

```bash
python -m server.main
```

```bash
python -m client.main
```

Run two clients and connect both to `127.0.0.1:5050`.
